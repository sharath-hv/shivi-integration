Euclid Circular B — add licensed webfonts here for @font-face in src/fonts.css.

Expected files (woff2 preferred; otf fallback):
  EuclidCircularB-Light.woff2 / .otf       (300)
  EuclidCircularB-Regular.woff2 / .otf     (400)
  EuclidCircularB-Medium.woff2 / .otf     (500)
  EuclidCircularB-SemiBold.woff2 / .otf    (600)
  EuclidCircularB-Bold.woff2 / .otf       (700)

See skills/primitives.md for CDN / design-token source.
